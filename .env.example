# Base URL of the Python REST backend.
# Used server-side only inside Next.js API proxy routes — never exposed to the browser.
# Point this to your local instance, a DevTunnel, or the production URL.
PYTHON_API_BASE_URL=http://127.0.0.1:80/
